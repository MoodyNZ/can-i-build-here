/**
 * Shared planning assumptions for the illustrative buildable-envelope figures.
 * These are placeholders for a generic residential zone, not real district-plan
 * rules — see the Disclaimer banner.
 */
export const SETBACK_METRES = 3;
export const MAX_COVERAGE_RATIO = 0.35;
